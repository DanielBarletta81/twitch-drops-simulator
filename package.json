{
  "name": "twitch-drops-simulator",
  "version": "1.0.0",
  "private": true,
  "description": "Real-time Twitch Drops simulation with event-driven backend and React dashboard",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "start": "concurrently \"npm run start:server\" \"npm run preview:client\"",
    "start:server": "cd server && npm start",
    "preview:client": "cd client && npm run preview",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}